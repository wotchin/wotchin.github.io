if page.tags
  -
    var currentLang = page.lang && String(page.lang).indexOf('zh') === 0 ? 'zh' : 'en';
    var tags = page.tags && page.tags.toArray ? page.tags.toArray() : [];
  .tags
    each tag in tags
      a.article-tag(href=url_for(currentLang === 'zh' ? 'zh/tags/' + (tag.slug || tag.name) + '/' : tag.path), title=tag.name)= tag.name
